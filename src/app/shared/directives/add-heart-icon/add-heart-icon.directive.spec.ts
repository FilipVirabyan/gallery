import { AddHeartIconDirective } from './add-heart-icon.directive';

describe('AddHeartIconDirective', () => {
  it('should create an instance', () => {
    const directive = new AddHeartIconDirective();
    expect(directive).toBeTruthy();
  });
});
